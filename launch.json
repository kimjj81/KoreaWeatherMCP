{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: 기상청 MCP 서버",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/weather_mcp.py",
            "console": "integratedTerminal",
            "justMyCode": false,
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}